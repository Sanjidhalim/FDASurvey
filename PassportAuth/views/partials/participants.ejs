<!DOCTYPE HTML>
<html>
<head>
    <title>Research Survey</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">

    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap-theme.min.css" integrity="sha384-fLW2N01lMqjakBkx3l/M9EahuwpSfeNvV63J5ezn3uZzapT0u7EYsXMjQV+0En5r" crossorigin="anonymous">
    <script src = "https://ajax.googleapis.com/ajax/libs/angularjs/1.3.0-beta.17/angular.min.js"></script>


</head>

<body>

<div class=container>

    <ul class="nav nav-tabs">
        <li role="presentation" class="active"><a href="/users">Home</a></li>
    </ul>

    <div ng-if="participants.length==0">
        <p> Click below to add participants to {{name}} </p>
    </div>

    <div ng-if="participants.length!=0">
        <p> <b>{{name}} </b> has the following participants. </p>
    </div>

    <table class="table">
        <tr ng-repeat="person in participants">
            <td>{{person}}</td>
            <td class="text-center">
                <button type="button" ng-click="deleteParticipant(person)" class="btn btn-default" aria-label="Left Align">
                    <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                </button>
            </td>
        </tr>
    </table>

        <form  class="form-horizontal">
            <div class="form-group row">
                <label for="inputEmail3" class="col-sm-2 control-label">Enter participant email</label>
                <div class="col-xs-4">
                    <input type="text" ng-model="email" class="form-control" name="participantEmail" placeholder="Email">
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit"  ng-click="submit(email)" class="btn btn-default">Submit</button>
                </div>
            </div>
        </form>

    <form method="post" action="/logout">
        <button  type="submit" class="btn btn-link">Log Out</button>
    </form>

</div>

</body>